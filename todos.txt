ou already have:
	•	Grammar skeleton (label, immediate, numeric_val)
	•	Transformer that produces simple AST nodes
	•	A working parse entry point

⸻

📋 TODO Breakdown (Parser → Assembler)

1. Grammar Expansion
	•	Instructions
	•	Add a rule for an instruction mnemonic (e.g. LDA, STA, etc.)
	•	Add addressing modes as sub-rules (immediate, absolute, zeropage, indirect, relative, etc.).
	•	Operands
	•	Define grammar for each operand form:
	•	Immediate: #value
	•	Absolute: $ADDR
	•	Zeropage: $00–$FF
	•	Indirect: ($ADDR)
	•	Indexed: $ADDR,X or $ADDR,Y
	•	Relative: label or offset for branch instructions.
	•	Directives (assembler-level)
	•	.ORG, .BYTE, .WORD, .INCLUDE, etc.

⸻

2. AST Representation
	•	Labels: {"type":"label","name":"FOO"}
	•	Instruction: {"type":"instruction","mnemonic":"LDA","operand":{...}}
	•	Directive nodes for .ORG, .BYTE, etc.
	•	Numeric values consistently normalized to integers (dec/hex/bin).

⸻

3. Semantic Layer
	•	Symbol table management
	•	Collect label definitions with their addresses.
	•	Detect duplicates / undefined symbols.
	•	Address calculation
	•	Keep a program counter (PC) during a first pass.
	•	Assign addresses to labels and instructions.
	•	Instruction size calculation
	•	Immediate = 2 bytes, Absolute = 3 bytes, etc.

⸻

4. Code Generation
	•	Opcode lookup
	•	Table mapping (mnemonic, addressing mode) → opcode byte.
	•	Operand encoding
	•	Decide number of bytes and endianness (6502 is little-endian).
	•	Byte emission
	•	Generate the final machine code stream.

⸻

5. Assembler Features
	•	Error reporting (invalid instruction, bad operand, undefined label).
	•	Listing output (show source with generated bytes).
	•	Output formats: raw binary, Intel HEX, maybe NES/iNES header for ROMs.

⸻

6. Extras (Optional, future “stories”)
	•	Macro support.
	•	Conditional assembly (.IF, .ELSE).
	•	Include/external files.
	•	Debug symbols export.

⸻

📌 Suggested Story Breakdown (Agile-ish)
	1.	MVP Parser
	•	Parse labels + immediates + numeric values.
	2.	Instruction Recognition
	•	Parse mnemonics with immediates.
	•	Add absolute/zeropage operands.
	3.	Branching
	•	Add relative addressing with labels.
	4.	Directives
	•	Support .ORG, .BYTE, .WORD.
	5.	First-pass assembler
	•	Symbol table, address assignment.
	6.	Code generation
	•	Emit correct opcodes and operands.
	7.	Error handling + tests
	•	Bad labels, bad addressing modes, range errors.
	8.	Output formats
	•	Raw binary → NES ROM, etc.

⸻

⚡That way you can make each of those into a story, and break them into branches (e.g. feature/add-absolute-addressing).

Would you like me to rewrite your current grammar into a more complete version (with all addressing modes stubbed out) so you have a foundation for the “MVP Parser” story?